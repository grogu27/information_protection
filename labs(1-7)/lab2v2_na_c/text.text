Задача: найти x такое, что 𝑎^𝑥≡𝑦(mod𝑝)

где
a — основание, 
y — результат, 
p — простое число (модуль).

Вместо того чтобы перебирать все степени подряд (O(p)), делим задачу на маленькие и большие шаги.

Теперь задача сводится к поиску пары i,j, такой что: 𝑎^(𝑖⋅𝑛)⋅𝑎^𝑗≡𝑦(mod𝑝)





y≡a^x(modp)
где
p — простое число,
a — основание (порождающий элемент группы),
y — значение, для которого ищем логарифм,
x — неизвестный дискретный логарифм.

1. Идея алгоритма
Прямой перебор (пересчитать 𝑎^1,𝑎^2,...,𝑎^𝑝) занимает 𝑂(𝑝) времени, что слишком долго.

Мы используем разбиение показателя 
x=i⋅n+j,
где 
𝑛=sqrt(p).
То есть i отвечает за «шаги великана», а j — за «шаги младенца».


2. Подготовка (Baby-step)

Вычисляем и сохраняем в таблицу все значения:

𝑎^𝑗 (mod𝑝), 𝑗=0,1,2,...n-1
Сохраняем пары (значение, j) для быстрого поиска (например, через хэш-таблицу).


3. Основной шаг (Giant-step)

Считаем обратный элемент для 𝑎^𝑛:
𝑎^−𝑛≡(𝑎^−1)^𝑛(mod𝑝)

Для этого используем расширенный алгоритм Евклида, чтобы найти обратный к 𝑎, и затем возводим в степень n.

Перебираем 𝑖=0,1,...,𝑛

Вычисляем:
γ=y⋅(a^−n)^i(modp).

Проверяем, есть ли γ в таблице «младенческих шагов».

Если нашли 𝛾=a^j, то: 𝑥 = 𝑖⋅𝑛+𝑗



4. Сложность

Память: 
𝑂(𝑝) (нужно хранить таблицу baby-steps).

Время: 
𝑂(𝑝) (baby-steps + giant-steps)

Это в тысячу раз быстрее, чем полный перебор 𝑂(𝑝), если p большое.