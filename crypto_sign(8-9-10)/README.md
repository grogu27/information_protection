# Лабораторные работы №8-10 по защите информации  
**Реализация электронной цифровой подписи (ЭЦП) по алгоритмам RSA, Эль-Гамаля и ГОСТ Р 34.10-94**

---

## 🎯 Цель работы

Разработка программы для создания и проверки электронной цифровой подписи файлов с использованием трёх криптографических алгоритмов:
- **RSA** (Лабораторная работа №8)
- **Эль-Гамаля** (Лабораторная работа №9) 
- **ГОСТ Р 34.10-94** (Лабораторная работа №10)

Программа вычисляет хеш-сумму SHA-256 файла и применяет выбранный алгоритм для подписи каждого байта хеша.

---

## 🚀 Быстрый старт

### Сборка проекта
```bash
make
```

### Использование
```bash
./sign_tool rsa document.pdf
./sign_tool elgamal document.pdf  
./sign_tool gost document.pdf
```

### Очистка
```bash
make clean
```

---

## 📁 Структура выходных файлов

Для входного файла `document.txt` программа создаёт:

**Файлы подписи:**
- `document.txt.sig` — бинарный файл с электронной подписью
- `document.txt.ver` — текстовый файл с результатом проверки (VALID/INVALID)

**Файлы ключей:**
- RSA: `private.pem`, `public.pem` (формат PEM)
- Эль-Гамаль: `elgamal_priv.key`, `elgamal_pub.key` (текстовый формат)
- ГОСТ: `gost_priv.key`, `gost_pub.key` (специальный формат)

**Форматы подписи:**
- **RSA**: блоки по 256 байт на каждый байт хеша
- **Эль-Гамаль**: пары (r,s) по 16 байт на каждый байт хеша
- **ГОСТ**: пары (r,s) в специальном формате

---

## 🧠 Теоретическая основа

### 🔐 Электронная цифровая подпись (ЭЦП)

ЭЦП — криптографический механизм, обеспечивающий:
- **🔑 Аутентификацию** — подтверждение авторства документа
- **🛡️ Целостность** — гарантия неизменности данных
- **📝 Неотказуемость** — невозможность отказа от подписи

**Общая схема работы:**
1. **Хеширование**: `H = Hash(M)`
2. **Подписание**: `S = Sign(H, private_key)`
3. **Верификация**: `Verify(H, S, public_key) → true/false`

> ⚠️ В реальных системах подписывается весь хеш, но по условию лабораторной работы каждый байт хеша подписывается отдельно.

### 🔢 Алгоритм RSA

**Генерация ключей:**
- Приватный ключ: `(d, n)`
- Публичный ключ: `(e, n)`

**Процесс подписи для байта b:**
```math
s ≡ b^d \mod n
```

**Проверка подписи:**
```math
b' ≡ s^e \mod n
```
Успех, если `b' = b`

### 🔢 Алгоритм Эль-Гамаля

**Параметры системы:**
- `p` — большое простое число  
- `g` — примитивный корень
- `x` — секретный ключ: `x ∈ [1, p−2]`
- `y` — открытый ключ: `y = g^x \mod p`

**Подпись байта m:**
1. Выбор случайного `k`, взаимно простого с `p−1`
2. Вычисление `r = g^k \mod p`
3. Вычисление `s = k⁻¹(m − x·r) \mod (p−1)`
4. Подпись: `(r, s)`

**Проверка подписи:**
```math
g^m ≡ y^r · r^s \pmod{p}
```
Успех, если сравнение выполняется

### 🇷🇺 Алгоритм ГОСТ Р 34.10-94

**Системные параметры:**
- `p` — большое простое число
- `q` — простой делитель `(p-1)` 
- `a` — образующий элемент: `a^q ≡ 1 \pmod{p}`

**Генерация ключей:**
- Закрытый ключ: `d ∈ (0, q)`
- Открытый ключ: `c = a^{-d} \mod p`

**Формирование подписи для хеша h:**
1. Выбор случайного `k ∈ (0, q)`
2. Вычисление `r = (a^k \mod p) \mod q`
3. Вычисление `s = (k·h + d·r) \mod q`
4. Подпись: `(r, s)`

**Проверка подписи:**
1. Вычисление `v = h^{-1} \mod q`
2. Вычисление `z₁ = s·v \mod q`
3. Вычисление `z₂ = (q - r)·v \mod q`
4. Вычисление `u = (a^{z₁} · c^{z₂} \mod p) \mod q`
5. Успех, если `u = r`

---

## ⚙️ Архитектура реализации

### 🔧 Модуль хеширования (utils.cpp)
```cpp
std::vector<unsigned char> sha256_file(const std::string& filename);
```
- Побайтовое чтение файла с буферизацией
- Вычисление SHA-256 через библиотеку OpenSSL
- Возврат хеш-суммы размером 32 байта

### 🔐 Реализация RSA (rsa_sign.cpp)
- Генерация 2048-битной RSA-пары ключей
- Последовательная подпись каждого байта хеша
- Сохранение подписей в бинарный файл
- Верификация путём чтения блоков по 256 байт

### 🔐 Реализация Эль-Гамаля (elgamal_sign.cpp)
- Генерация простого числа `p ≈ 1 000 000`
- Поиск примитивного корня `g` по модулю p
- Подпись каждого байта хеша с генерацией эфемерного ключа
- Упаковка пар (r,s) в 16-байтные блоки

### 🇷🇺 Реализация ГОСТ Р 34.10-94 (gost_sign.cpp)
- Инициализация криптографических параметров системы
- Генерация ключевой пары согласно стандарту
- Создание и верификация подписи по алгоритму ГОСТ
- Использование упрощённых параметров для учебных целей

---

## 📋 Особенности реализации

**Общие для всех алгоритмов:**
- Подпись каждого байта хеша SHA-256 отдельно
- Автоматическая проверка созданной подписи
- Сохранение результатов в стандартизированных форматах

**Специфические особенности:**
- RSA: использование 2048-битных ключей промышленного уровня
- Эль-Гамаль: работа с числами порядка 10! согласно условию задачи
- ГОСТ: учебная реализация с демонстрацией математического аппарата

---

## 🔍 Тестирование

Для проверки корректности работы:
1. Создайте тестовый файл
2. Выполните подпись выбранным алгоритмом
3. Убедитесь в создании файлов `.sig` и `.ver`
4. Проверьте наличие `VALID` в файле результата

**Пример тестирования:**
```bash
echo "Test content" > test.txt
./sign_tool rsa test.txt
cat test.txt.ver
```

---

## 📚 Дополнительная информация

- Все алгоритмы реализованы в соответствии с теоретическими основами криптографии
- Программа демонстрирует практическое применение ЭЦП
- Код может быть использован в образовательных целях для изучения криптографии

Для промышленного применения рекомендуется использование сертифицированных криптографических библиотек.