🔏 Что такое цифровая подпись (ЦП)

Цифровая подпись (ЦП) — это криптографический механизм, который:
✅ подтверждает подлинность отправителя (аутентичность),
🧱 гарантирует целостность данных (данные не были изменены),
🚫 исключает возможность отказа от авторства (невозможность отрицать подпись).

Цифровая подпись — это не просто шифрование, это доказательство, что сообщение действительно пришло от владельца секретного ключа.

⚙️ Как работает ЦП на основе RSA

RSA можно использовать в обратную сторону:

при шифровании открытым ключом (n, e),

при подписи — наоборот: подпись создаётся закрытым ключом (n, d),
а проверяется открытым (n, e).


🔧 Шаг 1. Генерация ключей

Выбираем два больших простых числа p и q
Вычисляем n = p * q
Вычисляем φ(n) = (p - 1)(q - 1)
Выбираем e, взаимно простое с φ(n)
Вычисляем d как обратный элемент к e по модулю φ(n)

🔑
Открытый ключ: (n, e)
Закрытый ключ: (n, d)

Шаг 2. Подписание сообщения

Пусть есть сообщение M.

Вычисляем хэш от сообщения:

h = H(M)


где H — криптографическая хэш-функция (например, SHA-256).

Подписываем хэш:

s = h^d mod n


Это и есть цифровая подпись s.

Отправляем (M, s) получателю.

🔍 Шаг 3. Проверка подписи

Получатель:

Вычисляет хэш от полученного сообщения:

h1 = H(M)


Расшифровывает подпись с помощью открытого ключа:

h2 = s^e mod n


Сравнивает:

если h1 == h2 → подпись верна ✅
иначе → сообщение подделано ❌


🧠 Интуиция

RSA-подпись — это способ "зашифровать хэш" закрытым ключом,
а проверить может любой, у кого есть открытый ключ.


🔒 Пример (на маленьких числах)

1️⃣ Пара ключей:

p = 61, q = 53 → n = 3233, φ(n) = 3120
e = 17, d = 2753


2️⃣ Сообщение: M = "HELLO"
Пусть H(M) = 123 (упрощённо)

3️⃣ Подпись:

s = 123^2753 mod 3233 = 855

4️⃣ Проверка:

s^17 mod 3233 = 123
совпадает с H(M) → подпись подлинная ✅

💡 Зачем нужна цифровая подпись
🧾 Подтверждение авторства — подписать может только владелец закрытого ключа.
🛡️ Целостность — любое изменение данных изменит хэш → подпись станет неверной.
🔍 Проверяемость — проверить может любой, у кого есть открытый ключ.